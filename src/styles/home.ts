import { StyleSheet } from "react-native";

export const HomeStyles = StyleSheet.create({
    AssigneeDialogContainer: {
        display: 'flex',
        gap: 8,
        marginTop: 8
    },
    AssigneeList: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        gap: 8
    }
})
