import { StyleSheet } from "react-native";

export const UpdateNicknameStyles = StyleSheet.create({
    container: {
        display: 'flex',
        gap: 16,
        marginTop: 32,
    },
    avatar: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 16,
    },
    nicknameInput: {
        marginBottom: 32,
    }
})